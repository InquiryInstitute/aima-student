# Thebe (live code in the book)

This page is part of the **same Jupyter Book** as the chapter exercises: one build (`_build/html`), one navigation model, and **Thebe** for in-browser execution after you click **Live Code** (Binder provides the kernel).

```{code-cell} ipython3
import sys
print(sys.version.split()[0])
```

Add more `{code-cell}` sections in MyST anywhere you want runnable Python alongside the markdown exercises.
