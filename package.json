{
  "name": "aima-assignment",
  "private": true,
  "description": "Student assignment repo for AIMA (from aima-assignment-starter template)",
  "scripts": {
    "grade": "node scripts/grade-with-bedrock.js",
    "docs:generate": "node scripts/generate-aima-docs.mjs",
    "book:build": "bash -lc 'test -x .venv/bin/jupyter-book && exec .venv/bin/jupyter-book build . || exec jupyter-book build .'",
    "book:serve": "bash scripts/serve-jupyter-book.sh"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.0.0"
  }
}
